import 'package:flutter/material.dart';

class ManageEducatorsPage extends StatelessWidget {
  const ManageEducatorsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Manage Educators (stub)'));
  }
}

